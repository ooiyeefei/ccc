# Changelog

## 0.2.0
- Bundled `rethink-survey-mcp-server` as MCP server. Installing this plugin auto-registers the MCP tools `design_survey_session`, `critique_survey`, `get_template`, `score_response`, `cluster_responses` and resources/prompts under the `rethink-survey` namespace.
- No breaking changes to existing commands or skill.

## 0.1.0
- Initial release with `/design-survey`, `/critique-survey`, `/turn-into-app` commands and the `rethink-surveys` skill.
